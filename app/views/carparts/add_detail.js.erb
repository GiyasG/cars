if ($("#carpart_detail0").length) {
  var n = $("#details").children().length
  $("#carpart_detail0").attr("id", "carpart_detail"+n)
  $("#carpart_detail"+n).attr("name", "carpart[detail"+n+"]")
  $("#carpart_detailname0").attr("id", "carpart_detailname"+n)
  $("#carpart_detailname"+n).attr("name", "carpart[detailname"+n+"]")
}
alert($("#details").children().length)
$("#details").append("<%= escape_javascript(render partial: 'carparts/add_detail', locals: { car: @car, nof: @number_of_details } ) %>");
